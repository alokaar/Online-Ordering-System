# Atlas (recommended): user / password / host — special characters in password are handled automatically
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_CLUSTER_HOST=
MONGODB_DB_NAME=food_ordering

# Local MongoDB: leave cluster fields empty and set:
# MONGODB_URI=mongodb://localhost:27017/

JWT_SECRET_KEY=
